% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_covariates_mice.R
\name{sample_covariates_mice}
\alias{sample_covariates_mice}
\title{Sample covariates using multivariate imputation using chained equations
(mice)}
\usage{
sample_covariates_mice(
  data,
  cat_covs = NULL,
  conditional = NULL,
  n_subjects = nrow(data),
  cont_method = "pmm",
  replicates = 1,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{data.frame (n x p) containing the original, observed,
time-invariant covariates (ID should not be included) that will be used to
inform the imputation.}

\item{cat_covs}{character vector containing the names of the categorical
covariates in orgCovs.}

\item{conditional}{list with conditional limits for sampled population. For
continuous covariates, specify a numeric vector of length 2 giving the
\code{c(min, max)} range, e.g. \code{list("WT" = c(40, 60), "BMI" = c(15, 25))}.
For categorical covariates (those listed in \code{cat_covs}), specify a character
vector of the allowed category values, e.g. \code{list("SEX" = c("F"))}.}

\item{n_subjects}{number of simulated subjects, default is the number of
subjects in the data.}

\item{cont_method}{method used to predict continuous covariates within mice,
default is \code{pmm}.}

\item{replicates}{number of multiple imputations replicates to sample.
Default is 1.}

\item{seed}{integer random seed passed to \code{\link[=set.seed]{set.seed()}} for reproducibility.
Default \code{NULL} does not set a seed.}

\item{...}{additional arguments passed to \code{mice::mice()} function}
}
\value{
data.frame with the simulated covariates, with n_subjects * m
rows and p columns
}
\description{
In contrast to sampling methods bootstrap and NHANES, categorical covariates
need to be specified using the \code{cat_covs} argument, otherwise they will be
treated as continuous.
}
\details{
missing values in \code{data} must be coded as NA
}
