% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_covariates_copulas_timevarying.R
\name{sample_covariates_copulas_timevarying}
\alias{sample_covariates_copulas_timevarying}
\title{Sample time-varying covariates using copula modeling}
\usage{
sample_covariates_copulas_timevarying(
  data,
  id_var = "ID",
  time_var = "TIME",
  time_grid = NULL,
  static_covs = NULL,
  time_varying_covs = NULL,
  degree = 1,
  family_set = "all",
  selcrit = "aic",
  bw_mult = 1,
  noise = NULL,
  truncate = TRUE,
  n_subjects = length(unique(data[[id_var]])),
  replicates = 1,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{long-format \code{data.frame} or \code{tibble} containing one row per
subject and time.}

\item{id_var}{subject identifier column name.}

\item{time_var}{time column name. Must be numeric.}

\item{time_grid}{optional numeric vector of simulated times. If supplied,
every simulated subject's trajectory is reconstructed on this grid. If
\code{NULL} (default), each simulated subject clones an observed subject's
observation-time vector, assigned deterministically in observed subject
order and recycled if \code{n_subjects} exceeds the number of observed subjects.}

\item{static_covs}{character vector of continuous covariates that are
constant within a subject. Sampled jointly with the trajectory coefficients
and carried forward unchanged.}

\item{time_varying_covs}{character vector of continuous covariates to model
over time. Default is all columns except \code{id_var}, \code{time_var}, and
\code{static_covs}.}

\item{degree}{degree of the per-subject polynomial in \code{time_var} used to
summarise each time-varying covariate trajectory. Default \code{1} (a
subject-specific intercept and slope). Higher degrees capture curvature but
require more timepoints per subject.}

\item{family_set}{copula families considered by \code{rvinecopulib::vine()}.
Default \code{"all"}.}

\item{selcrit}{model-selection criterion for the vine copula, default
\code{"aic"}. Use \code{"loglik"} for the tightest in-sample reproduction.}

\item{bw_mult}{bandwidth multiplier for the kernel-density marginals passed to
\code{rvinecopulib::vine()} (\code{margins_controls$mult}). Default \code{1}. Values below
\code{1} reduce marginal smoothing, tightening the simulated marginals toward the
observed (empirical) distribution; this is the main lever for matching the
original covariate distributions most closely. Very small values risk
overfitting the observed sample.}

\item{noise}{optional single non-negative number giving the log-scale SD of
multiplicative log-normal noise (e.g. \code{0.05} for ~5\%) applied to the
reconstructed output values to obscure the generated data. \code{NULL} (default)
applies no noise. Time-varying covariates are jittered independently at each
timepoint (adding measurement-like scatter around the trajectory); static
covariates are jittered once per subject so they stay constant over time.
Applied before \code{truncate}.}

\item{truncate}{logical; if \code{TRUE} (default) reconstructed time-varying
covariate values are clamped to the observed \verb{[min, max]} range of each
covariate, preventing nonphysical extrapolated values.}

\item{n_subjects}{number of simulated subjects. Default is the number of
unique observed subjects.}

\item{replicates}{number of independent simulated datasets to sample.
Default is 1. When greater than 1, a \code{.replicate} column is included.}

\item{seed}{integer random seed passed to \code{\link[=set.seed]{set.seed()}} for reproducibility.
Default \code{NULL} does not set a seed.}

\item{...}{reserved for future use.}
}
\value{
long-format data.frame with simulated subject IDs, time, static
covariates, and time-varying covariates.
}
\description{
Implements the copula-based virtual-patient simulation of Zwep et al. (2024)
for time-varying covariates. Each continuous time-varying covariate trajectory
is summarised by the per-subject coefficients of a random-effects polynomial
regression on time; a vine copula is then fitted to the joint distribution of
those coefficients together with the (continuous) static covariates. New
virtual subjects are drawn from the copula and their trajectories are
reconstructed by evaluating the sampled polynomial coefficients over a time
grid. Modelling dependence in coefficient space, with nonparametric (kernel)
marginals, reproduces the realistic covariate domain and the full correlation
structure without resampling or extrapolating individual observed values.
}
\details{
The algorithm follows Zwep et al. (2024):
\enumerate{
\item For each time-varying covariate \code{X_j}, fit a random-effects polynomial
regression \code{X_j ~ poly(time, degree) + (poly(time, degree) | id)} and
extract the per-subject coefficient vector. If the full random-slope model
fails to converge, a random-intercept model is used and the
population-level slope coefficients are shared across subjects.
\item Assemble a subject-level table of all trajectory coefficients and the
static covariates, and fit a vine copula with kernel-density marginals via
\code{rvinecopulib::vine()}.
\item Draw \code{n_subjects} coefficient vectors from the copula and reconstruct each
trajectory by evaluating its polynomial over the requested times.
}

Only continuous covariates are supported; categorical covariates should be
handled with \code{\link[=sample_covariates_mice_timevarying]{sample_covariates_mice_timevarying()}}.

Requires the \code{rvinecopulib} package.
}
\references{
Zwep LB, Guo T, Nagler T, Knibbe CAJ, Meulman JJ, van Hasselt JGC. Virtual
Patient Simulation Using Copula Modeling. Clin Pharmacol Ther.
2024;115(4):795-804. \doi{10.1002/cpt.3099}
}
\seealso{
\code{\link[=sample_covariates_mice_timevarying]{sample_covariates_mice_timevarying()}} and
\code{\link[=sample_covariates_lme_timevarying]{sample_covariates_lme_timevarying()}} for the resampling and mixed-effects
alternatives.
}
