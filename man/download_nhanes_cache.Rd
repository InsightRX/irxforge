% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_nhanes_cache.R
\name{download_nhanes_cache}
\alias{download_nhanes_cache}
\title{Download and cache NHANES data locally}
\usage{
download_nhanes_cache(
  groups = c("DEMO", "LAB", "EXAM"),
  years = "2017-2018",
  path = nhanes_default_cache_dir(),
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{groups}{character vector of NHANES data groups to download. Valid
values: \code{"DEMO"} (Demographics), \code{"LAB"} (Laboratory), \code{"EXAM"}
(Examination), \code{"Q"} (Questionnaire), \code{"DIET"} (Dietary).
Defaults to \code{c("DEMO", "LAB", "EXAM")}.}

\item{years}{character vector of NHANES survey cycles, e.g.
\code{c("2015-2016", "2017-2018")}. Defaults to \code{"2017-2018"}.}

\item{path}{directory where merged RDS files will be saved. Created
automatically if it does not exist. Defaults to the package-level cache
directory returned by \code{nhanes_default_cache_dir()}.}

\item{overwrite}{logical. If \code{FALSE} (default), a year that already has a
merged RDS in \code{path} is skipped. Set to \code{TRUE} to re-download and
overwrite.}

\item{...}{additional arguments (currently unused)}
}
\value{
the \code{path} directory, invisibly.
}
\description{
Downloads all tables within the specified NHANES data groups for one or
more survey years, merges them into a single data frame per year, and
saves it as an RDS file. The resulting cache is used automatically by
\code{\link[=sample_covariates_nhanes]{sample_covariates_nhanes()}}.
}
\details{
Each survey year is saved as a single file \verb{nhanes_<year>.rds}
(e.g. \verb{nhanes_2017-2018.rds}) containing all variables from all downloaded
tables, merged on the SEQN respondent sequence number. Tables with
multiple rows per subject (e.g. dietary recall) are skipped automatically.

Requires the \code{nhanesA} package.
}
