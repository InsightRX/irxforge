% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_nm_dataset.R
\name{check_nm_dataset}
\alias{check_nm_dataset}
\title{Run automated quality checks on a NONMEM dataset}
\usage{
check_nm_dataset(
  data,
  dictionary = NULL,
  exclusion_flag = NULL,
  max_digits = 10,
  max_id_digits = 5,
  extra_reserved = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{a data.frame (or tibble) containing the NONMEM dataset to check.}

\item{dictionary}{an optional named list that maps standard NONMEM column
names to the actual column names in \code{data}. For example,
\code{list(ID = "NMID", TIME = "TAD")} means the column called \code{"NMID"} in
\code{data} plays the role of \code{ID}, and \code{"TAD"} plays the role of \code{TIME}.
Any standard name not listed in the dictionary is assumed to match the
column name in \code{data} as-is. Supported keys: \code{ID}, \code{TIME}, \code{DV}, \code{MDV},
\code{EVID}, \code{AMT}, \code{CMT}, \code{RATE}, \code{SS}, \code{ADDL}, \code{II}.}

\item{exclusion_flag}{character name of the column used as an exclusion flag
(for \verb{$DATA ACCEPT} / \code{IGNORE} in NONMEM). If \code{NULL} (default), the
exclusion-flag check is skipped.}

\item{max_digits}{integer; threshold for flagging numbers with too many
significant digits (default \code{10}).}

\item{max_id_digits}{integer; preferred maximum number of characters for the
ID variable (default \code{5}).}

\item{extra_reserved}{character vector of additional reserved NONMEM names
to check for (e.g. PK parameters used in ADVAN/TRANS such as \code{"CL"},
\code{"V"}, \code{"V1"}, \code{"K12"}).}

\item{verbose}{logical; if \code{TRUE} (default), messages are printed for every
check as it is evaluated.}
}
\value{
A data.frame with columns \code{check} (short test name), \code{description}
(human-readable explanation), and \code{result} (\code{"PASS"}, \code{"FAIL"}, or
\code{"NA"} when a check is not applicable).
}
\description{
Performs a comprehensive set of checks on a NONMEM-style dataset and returns
a summary table with the result of each check. No errors are thrown; all
issues are reported as rows in the output table.
}
