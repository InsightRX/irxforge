% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_covariates_copulas.R
\name{sample_covariates_copulas}
\alias{sample_covariates_copulas}
\title{Sample covariates using copula modeling}
\usage{
sample_covariates_copulas(
  data,
  conditional = NULL,
  n_subjects = nrow(data),
  family_set = "all",
  selcrit = "aic",
  bw_mult = 1,
  truncate = TRUE,
  replicates = 1,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{data.frame or tibble (n x p) of observed, time-invariant
covariates, one row per subject (an ID column should not be included). Only
continuous covariates are supported.}

\item{conditional}{optional named list of \code{c(min, max)} ranges restricting the
sampled population, e.g. \code{list(AGE = c(60, 80), WT = c(70, 100))}. The
observed data are filtered to these ranges before the copula is fitted.}

\item{n_subjects}{number of simulated subjects. Default is the number of rows
in \code{data}. Unlike resampling methods, the copula is generative, so
\code{n_subjects} may exceed the number of observed subjects.}

\item{family_set}{copula families considered by \code{rvinecopulib::vine()}.
Default \code{"all"}.}

\item{selcrit}{model-selection criterion for the vine copula, default
\code{"aic"}. Use \code{"loglik"} for the tightest in-sample reproduction.}

\item{bw_mult}{bandwidth multiplier for the kernel-density marginals passed to
\code{rvinecopulib::vine()} (\code{margins_controls$mult}). Default \code{1}. Values below
\code{1} reduce marginal smoothing, tightening the simulated marginals toward the
observed distribution.}

\item{truncate}{logical; if \code{TRUE} (default) sampled covariate values are
clamped to the observed \verb{[min, max]} range of each covariate, preventing
nonphysical extrapolated values.}

\item{replicates}{number of independent simulated datasets to sample. Default
is 1. When greater than 1, a \code{.replicate} column is included.}

\item{seed}{integer random seed passed to \code{\link[=set.seed]{set.seed()}} for reproducibility.
Default \code{NULL} does not set a seed.}

\item{...}{reserved for future use.}
}
\value{
data.frame with the simulated covariates, with \code{n_subjects} rows
(times \code{replicates}) and \code{p} columns.
}
\description{
Implements the copula-based virtual-patient covariate simulation of Zwep et
al. (2024). A vine copula with kernel-density marginals is fitted to the
observed covariates and new virtual subjects are drawn from it, reproducing
both the individual covariate distributions and their full dependence
structure. This is the cross-sectional (time-invariant) counterpart of
\code{\link[=sample_covariates_copulas_timevarying]{sample_covariates_copulas_timevarying()}}.
}
\details{
Only continuous covariates are supported; categorical covariates should be
handled with \code{\link[=sample_covariates_mice]{sample_covariates_mice()}}. Rows with missing values are dropped
before fitting. Covariates that are constant across the (filtered) data are
held fixed and re-attached after sampling.

Requires the \code{rvinecopulib} package.
}
\references{
Zwep LB, Guo T, Nagler T, Knibbe CAJ, Meulman JJ, van Hasselt JGC. Virtual
Patient Simulation Using Copula Modeling. Clin Pharmacol Ther.
2024;115(4):795-804. \doi{10.1002/cpt.3099}
}
\seealso{
\code{\link[=sample_covariates_copulas_timevarying]{sample_covariates_copulas_timevarying()}} for the time-varying
variant.
}
