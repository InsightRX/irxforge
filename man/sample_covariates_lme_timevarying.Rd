% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_covariates_lme_timevarying.R
\name{sample_covariates_lme_timevarying}
\alias{sample_covariates_lme_timevarying}
\title{Sample time-varying covariates using linear mixed-effects transition models}
\usage{
sample_covariates_lme_timevarying(
  data,
  id_var = "ID",
  time_var = "TIME",
  time_grid = NULL,
  static_covs = NULL,
  time_varying_covs = NULL,
  cat_covs = NULL,
  trend = c("previous", "time"),
  random_intercept = TRUE,
  measurement_pattern = c("change", "nonmissing", "all"),
  design_match = c("clone", "propensity"),
  design_match_covs = NULL,
  n_subjects = length(unique(data[[id_var]])),
  baseline_method = c("mice", "bootstrap"),
  noise = NULL,
  conditional = NULL,
  cont_method = "pmm",
  replicates = 1,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{long-format \code{data.frame} or \code{tibble} containing one row per
subject and time.}

\item{id_var}{subject identifier column name.}

\item{time_var}{time column name.}

\item{time_grid}{optional numeric vector of simulated times. If supplied,
every simulated subject uses this same time grid. If \code{NULL} (default), each
simulated subject clones an observed subject's covariate observation-time
design. Designs are assigned deterministically in observed subject order
and recycled if \code{n_subjects} exceeds the number of observed subjects.}

\item{static_covs}{character vector of covariates that should be sampled at
baseline and then carried forward unchanged. Categorical covariates are
only supported as static covariates (see \code{cat_covs}).}

\item{time_varying_covs}{character vector of covariates to sample at each
timepoint. Default is all columns except \code{id_var}, \code{time_var}, and
\code{static_covs}. Time-varying covariates must be continuous.}

\item{cat_covs}{character vector containing categorical covariates. These may
only appear among \code{static_covs}; categorical time-varying covariates are
not supported by the mixed-effects engine.}

\item{trend}{predictors used to model each transition. \code{"previous"} (default)
regresses each covariate on the previous covariate values and the elapsed
time only (a Markov / autoregressive transition). \code{"time"} additionally
includes absolute \code{time_var} as a fixed effect, capturing a population time
trajectory on top of the dependence on the previous value.}

\item{random_intercept}{logical, whether to include a subject-level random
intercept (default \code{TRUE}). When \code{FALSE}, or when the mixed-effects fit
fails to converge, the transition is modelled with ordinary least squares.}

\item{measurement_pattern}{how to determine when a time-varying covariate
receives a new measurement in the sampled observed profile. \code{"change"}
(default) treats a covariate as newly measured when its value changes from
the previous row, which is useful for datasets where covariates have been
carried forward. \code{"nonmissing"} treats non-missing values as measurements.
\code{"all"} updates every time-varying covariate at every sampled row.}

\item{design_match}{method used to assign observed covariate observation-time
designs to simulated subjects when \code{time_grid = NULL}. \code{"clone"} (default)
assigns designs deterministically in observed subject order. \code{"propensity"}
fits a logistic propensity model comparing observed baseline covariates to
sampled baseline covariates, then assigns each simulated subject the design
from the unmatched observed subject with the nearest propensity score.}

\item{design_match_covs}{character vector of baseline covariates to use for
propensity-score design matching. Default is all static and time-varying
covariates.}

\item{n_subjects}{number of simulated subjects. Default is the number of
unique observed subjects.}

\item{baseline_method}{how baseline covariates are sampled. \code{"mice"}
(default) generates baselines with \code{\link[=sample_covariates_mice]{sample_covariates_mice()}}. \code{"bootstrap"}
resamples observed baseline rows with replacement via
\code{\link[=sample_covariates_bootstrap]{sample_covariates_bootstrap()}}, which reproduces the observed joint
baseline distribution instead of shrinking it toward the multivariate mean
as full-row MICE imputation tends to, especially in small datasets.}

\item{noise}{optional single non-negative number giving the log-scale SD of
multiplicative log-normal noise applied to the \strong{baseline} continuous
covariates (e.g. \code{0.05} for ~5\%) to obscure original values. \code{NULL}
(default) applies no noise. Sequential transitions are not jittered.}

\item{conditional}{list with conditional limits applied to the \strong{baseline}
covariate sample. Sequential transitions are not constrained. See
\code{\link[=sample_covariates_mice]{sample_covariates_mice()}} for the accepted format.}

\item{cont_method}{method used to predict continuous covariates within mice
for the baseline sample, default is \code{pmm}.}

\item{replicates}{number of independent simulated datasets to sample.
Default is 1. When greater than 1, a \code{.replicate} column is included.}

\item{seed}{integer random seed passed to \code{\link[=set.seed]{set.seed()}} for reproducibility.
Default \code{NULL} does not set a seed.}

\item{...}{additional arguments passed to \code{\link[=sample_covariates_mice]{sample_covariates_mice()}} for the
baseline sample.}
}
\value{
long-format data.frame with simulated subject IDs, time, static
covariates, and time-varying covariates.
}
\description{
Samples baseline covariates with \code{\link[=sample_covariates_mice]{sample_covariates_mice()}}, then samples
later timepoints sequentially from per-covariate linear mixed-effects models
fitted to the observed first-order transitions. Each continuous time-varying
covariate is modelled as a function of the previous values of all
time-varying covariates (capturing both autocorrelation and between-covariate
relationships), the elapsed time since the previous measurement, the static
covariates and, optionally, absolute time. A subject-level random intercept
captures within-subject correlation; new random effects are drawn for each
simulated subject.
}
\details{
This function implements a first-order linear mixed-effects transition
sampler. For each continuous time-varying covariate \code{X_j} it fits
\code{X_j(t) ~ X_1(t-1) + ... + X_p(t-1) + delta_time [+ time] + static + (1 | id)}
to the observed transitions, then generates simulated trajectories by drawing
a fresh random intercept per simulated subject and adding residual noise at
each timepoint.

In contrast to \code{\link[=sample_covariates_mice_timevarying]{sample_covariates_mice_timevarying()}}, which resamples
observed values via chained equations, this sampler interpolates and
extrapolates through the fitted linear models and so can produce covariate
values not present in the observed data. It is intended as a lightweight
parametric alternative when smooth, model-based trajectories are preferred.

Missing values in \code{data} must be coded as \code{NA}. Transitions with missing
predictors or response are dropped when fitting the models.
}
\seealso{
\code{\link[=sample_covariates_mice_timevarying]{sample_covariates_mice_timevarying()}} for the chained-equations
resampling alternative.
}
