% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reformat_data_nca_to_modeling.R
\name{reformat_data_nca_to_modeling}
\alias{reformat_data_nca_to_modeling}
\title{Reformat NCA-type analysis-ready dataset (ARD) into a NONMEM-style modeling
dataset}
\usage{
reformat_data_nca_to_modeling(
  data,
  dictionary = list(subject_id = "ID", group = "GROUP", time = "TIME", dose = "AMT", conc
    = "DV"),
  dose_compartment = 1,
  obs_compartment = 1,
  covariates = NULL,
  repeat_doses = NULL,
  na = "."
)
}
\arguments{
\item{data}{dataset formatted as NCA analysis-ready dataset}

\item{dictionary}{a data dictionary that maps expected variable names to
variables in the data.}

\item{dose_compartment}{the compartment in which doses are entered}

\item{obs_compartment}{the observation compartment number}

\item{covariates}{a vector of covariate names that are to be extracted
and added to the modeling dataset.}

\item{repeat_doses}{Optional list for repeated dosing (MAD studies). Must
contain \code{interval} (dosing interval in TIME units). Optionally contains \code{n}
(total number of doses). If \code{n} is omitted, it is inferred per subject/group
as \code{ceiling(max(observation_time) / interval)}. Only applies to column-wise
dose data. Default \code{NULL} preserves existing behavior (no ADDL/II columns).
Examples: \code{list(interval = 12)} or \code{list(n = 5, interval = 12)}.}

\item{na}{what to set NA values to. E.g. ".", (default) or NA (keep NA),
or NULL (do nothing).}
}
\value{
data.frame with population PK input data in NONMEM-style
format.
}
\description{
Reformat NCA-type analysis-ready dataset (ARD) into a NONMEM-style modeling
dataset
}
