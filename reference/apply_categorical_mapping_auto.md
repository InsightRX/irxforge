# Build an automatic frequency-based mapping

Build an automatic frequency-based mapping

## Usage

``` r
apply_categorical_mapping_auto(data, columns)
```

## Arguments

- data:

  A data.frame

- columns:

  Character vector of column names

## Value

A data.frame with columns `column`, `original_value`, `encoded_value`
