# Example dataset with timepoints in a NONMEM dataset

Example dataset with timepoints in a NONMEM dataset

## Usage

``` r
nm_data_timepoints
```

## Format

An object of class `data.frame` with 3600 rows and 4 columns.
